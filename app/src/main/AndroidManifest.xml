<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Feedbacksystem"
        tools:targetApi="31">

        <activity android:name=".admin.AdminDashboardActivity"/>
        <activity android:name=".admin.QuestionsListActivity" />
        <activity android:name=".admin.TrainersListActivity"/>
        <activity android:name=".user.DebugActivity"/>

        <activity android:name=".user.LoginActivity" />
        <activity android:name=".user.RegisterActivity" />
        <activity android:name=".user.UserDashboardActivity" />
        <activity android:name=".user.ModuleFeedbackActivity" />
        <activity android:name=".user.TrainerSelectionActivity" />
        <activity android:name=".user.ModuleSelectionActivity" />
        <activity android:name=".user.QuestionActivity" />
        <activity android:name=".user.FeedbackPreviewActivity" />
        <activity android:name=".user.FeedbackSubmitActivity" />
        <activity android:name=".user.ThankYouActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Feedbacksystem">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>